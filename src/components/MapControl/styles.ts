import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    position: "absolute",
    top: 0,
    left: 0,
  },
  btn: {
    outline: "none",
    position: "absolute",
  },
});
