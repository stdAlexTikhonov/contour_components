import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  reportHeader: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  caption: {
    flexGrow: 1,
    padding: 10,
  },
});
